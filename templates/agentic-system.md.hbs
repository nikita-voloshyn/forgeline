# {{projectName}} — Agentic System

> Auto-generated by [Forgeline](https://github.com/nikita-voloshyn/forgeline). Do not edit manually — re-run `/setup-agents` to regenerate.

## Overview

This document describes the multi-agent development system configured for **{{projectName}}**.

## Architecture

```mermaid
flowchart TD
{{#each agents}}
    {{name}}["{{displayName}}\n({{model}})"]
{{/each}}

{{#each agents}}
{{#each owns}}
    {{../name}} --> owns_{{@../index}}_{{@index}}["{{this}}"]
{{/each}}
{{/each}}
```

## Agents

{{#each agents}}
### {{displayName}}

- **Model:** `{{model}}`
- **Domain:** {{domain}}
- **File:** `agents/{{name}}.md`

**Owns:**
{{#each owns}}
- `{{this}}`
{{/each}}

**Forbidden from:**
{{#each forbidden}}
- `{{this}}`
{{/each}}

**Verification:**
{{#each verification}}
- `{{this}}`
{{/each}}

---

{{/each}}

## Skills

| Skill | Purpose |
|-------|---------|
{{#each skills}}
| `/{{name}}` | {{description}} |
{{/each}}

## Hooks

### PostToolUse — Auto-lint

Triggers after every file edit (`Edit|Write|MultiEdit`).

```
{{hooks.postToolUse.command}}
```

### Stop — Secrets Scan

Triggers at session end.

```
{{hooks.stop.command}}
```

## Permissions

### Allow (`.claude/settings.local.json`)

{{#each permissions.allow}}
- `{{this}}`
{{/each}}

### Deny (`.claude/settings.json`)

{{#each permissions.deny}}
- `{{this}}`
{{/each}}

## Plugins

{{#each plugins}}
- **{{name}}**{{#if reason}} — {{reason}}{{/if}}
{{/each}}
