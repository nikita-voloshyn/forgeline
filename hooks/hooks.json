{
  "PostToolUse": [
    {
      "matcher": "Edit|Write|MultiEdit",
      "description": "Validate JSON syntax after config file edits",
      "hooks": [
        {
          "type": "command",
          "command": "case \"$CLAUDE_FILE_PATH\" in *.json) python3 -m json.tool \"$CLAUDE_FILE_PATH\" > /dev/null 2>&1 || echo '[Forgeline] Invalid JSON: '\"$CLAUDE_FILE_PATH\" ;; esac"
        }
      ]
    }
  ]
}
